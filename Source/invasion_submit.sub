#!/bin/bash
 
#$ -S /bin/bash
#$ -N allargs
#$ -l h_rt=60,h_vmem=1G 
#$ -binding linear:1
 
# Output files
#$ -o /work/$USER/$JOB_NAME-$JOB_ID.out
#$ -e /work/$USER/$JOB_NAME-$JOB_ID.err
 
for arg in "$@" ; do
  echo $arg
done
